using System.ComponentModel.DataAnnotations;

namespace BlogIT.MVC.ViewModels
{
    public enum GenderList
    {
        [Display(Name = "M")]
        M,
        [Display(Name = "F")]
        F
    }
}
